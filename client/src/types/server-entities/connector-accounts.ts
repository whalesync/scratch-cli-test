// TestConnectionResponse, ApiQuotaResponse, RevealCredentialsResponse are the
// shared contract — re-exported so existing importers keep working.
export type { ApiQuotaResponse, RevealCredentialsResponse, TestConnectionResponse } from '@spinner/shared-types';
