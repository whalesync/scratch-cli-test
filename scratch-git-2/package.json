{
  "name": "scratch-git-2",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "GIT_REPOS_DIR=\"$INIT_CWD/../local/scratch-git-repos\" cargo run",
    "build": "cargo build --release 2>&1 | tail -5",
    "format": "cargo fmt",
    "test": "cargo test"
  }
}
