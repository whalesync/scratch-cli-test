{
  "schema": {
    "type": "object",
    "required": ["title"],
    "properties": {
      "title": { "type": "string", "description": "Required non-nullable string — a null on a required non-nullable field is flagged as missing" }
    }
  }
}
