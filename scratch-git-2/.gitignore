/target
target-cli-integration/
.cargo-cli-integration/
repos/
cli-binaries/

# napi-rs cdylib artifacts (slice H.2 of DEV-10144). Built locally via
# scratch-desktop/scripts/build-native.cjs; the .node is renamed from the
# dylib in target/release/. Don't commit binaries.
napi/*.node
