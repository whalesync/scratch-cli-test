# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
lerna-debug.log*

# Diagnostic reports (https://nodejs.org/api/report.html)
report.[0-9]*.[0-9]*.[0-9]*.[0-9]*.json

# Just works!
.DS_Store

# Terraform state files
.terraform/
*.tfstate
*.tfstate.backup
*.tfstate.lock.info
local

# Environment
.env
.env.*
!.env.example
!.env.integration.example
!.env.connector-build.sample
!connector-build/.env.connector-build.sample

# Claude local files.
settings.local.json

# Agent/editor local context directories (at any depth) — never commit their contents.
.context/

# Node.js compiled output and packages.
dist/
node_modules/
*.tsbuildinfo

# Python bytecode caches
__pycache__/
*.pyc

# Turborepo cache.
.turbo/

# Scratch CLI hidden dir.
.scratchmd/

# Integration test local env files
.env.integration

# Docker image exports from CI builds.
*.tar

# Developer local files
.local

# Local repos (e.g. scratch-git test data)
repos/

.claude/worktrees
.claude/scheduled_tasks.lock

# Test report artifacts
junit.xml

# CLI integration CI: isolated cargo home at repo root (see gitlab-ci/common.yml)
.cargo-cli-integration/

# Cargo workspace build directory
/target

# Experimental scratch CLI (Rust) - removed but may have local build artifacts
experimental/scratch-cli-2/target/

# Napi-RS native addons
*.node

# Local dev data
local/

# Client local artifacts
client/.auth/
client/.clerk/.tmp/
client/playwright-report/
client/test-results/
client/next-env.d.ts

# Gitlab shell runner
/builds

# scratch-desktop release script
/scratch-desktop/dist-release
/scratch-desktop/dist-release-test

.gstack/
.conductor/

# design-sync (Claude Design converter) — transient build/verification state
.design-sync/sb-reference/
.design-sync/learnings/
.design-sync/.cache/
.design-sync/node_modules
.ds-sync/
ds-bundle/
