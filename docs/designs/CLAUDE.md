# Milestones

Each milestone covers a chunk of work for the team in a week or two, documented in `M*` folders. The top level contains things that span multiple milestones.

## Wireframes

For wireframing tasks, generate `.excalidraw` files in the same folder as the related docs. See `EXCALIDRAW.md` for the file format reference, color palette, and VS Code setup.
