out/
dist/
dist-release/
dist-release-test/
resources/bin/

# Bundled git binaries fetched by scripts/download-git.cjs (DEV-10196).
.git-bundle/
.git-bundle-cache/

.env
.env.*
!.env.example*
node_modules/

# electron-vite writes a temp bundled config file next to electron.vite.config.ts
# on every load and unlinks it afterwards; if the process is killed mid-load it
# can be left behind. Ignore it so it never gets committed.
electron.vite.config.*.mjs
