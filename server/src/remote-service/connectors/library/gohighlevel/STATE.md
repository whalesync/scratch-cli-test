<!-- ⛔ DO NOT DELETE THIS FILE. Generated/maintained by the `/connector-build` skill as the
     resumable record of this connector's live-test coverage. The connector work needs it to
     finish/extend the connector. Update it in place; never remove it. -->

<!-- TRIAL banner goes here at the very top IF the GHL test account is on a paid trial.
     Status currently unknown — verify in GHL billing and add a "CANCEL BY <date>" banner if so. -->

# GoHighLevel (HighLevel) — Connector Test Coverage

> **Do not delete this file.** Generated by `/connector-build`; the resumable record of what's been tested.

## Test account (used to run this coverage)
- **Service:** HighLevel sub-account ("Location") `57eAggUmMecWhpP8kkis`
- **Trial:** unknown — verify in GHL → Billing; add a "CANCEL BY <date>" banner at the top if it's a paid trial.
- **Credentials:** Private Integration Token (`apiKey`, `pit-...`) + `locationId`. *Token omitted here.* Get it by decrypting `ConnectorAccount.encryptedCredentials` for `coa_Avlneqisk5` (AES-256-GCM, key `ENCRYPTION_MASTER_KEY` in server/.env, AAD `connector-account`; see `server/tools/decrypt-credentials.js`), or mint a new one in HighLevel → **Settings → Private Integrations** (scopes: `contacts.readonly`, `opportunities.readonly`, `locations/customFields.readonly`, + write scopes for push).
- **Scratch side:** workbook `wkb_AWTkWjganc` ("My workspace") / connection `coa_Avlneqisk5` · `Health: OK`
- **Auth method:** `user_provided_params` (PIT + locationId) — CLI-connectable.

## Metadata
- **Type:** **MIXED** — static system fields + **dynamic custom fields per Location** (Contacts/Opportunities) + fully-dynamic **Custom Objects**.
- **Last run:** 2026-06-05 · scratchmd CLI pull pass (Pass 1 breadth, ~10 min) · Tester: Ivan

> Pass 1 results (2026-06-05): pulled Contacts, Opportunities, Pipelines, Users — all verbatim. Opportunity FK **read** verified end-to-end (`pipelineId` → "Ivan's 1st pipeline", `contactId` → the pulled Contact `3MNJodIs74s23TcFT4M8`). Proposals pulled 0 (empty account **or** the limit-cap silent-fail — verify). Note: the shared "My workspace" clone was wedged by stale Airtable edits; used a fresh clone at `local/ghl-clean/My workspace`.
>
> Browser pass (2026-06-05): **Contacts Create→Pull verified** — created "ScratchBrowser GHLtest 0605" in the GHL UI (id `3eaHAabkRTH3ehabUKFF`) → `linked pull` → landed verbatim as `scratchbrowser-ghltest-0605.json`. Account confirmed logged in (white-labeled "MOGXP", WhaleSync / Redmond WA).
>
> Deep session (2026-06-05, ~30 min):
> - **Contacts Edit→Push ✅** — set `companyName`/`city` → accept/upload/publish → verified via **re-pull** (connector read). ⚠️ GHL `GET /contacts/{id}` *omits* `companyName`/`city`/`businessName`, so verify contact writes via the **search/pull**, not GET-by-id (the GET nearly produced a false "didn't land").
> - **FK create on Scratch side ✅✅** (the headline) — wrote a new Opportunity `scratch-fk-opp-0605.json` with `pipelineId`+`pipelineStageId`+`contactId` (→ the browser-created contact) → published → GHL API confirms opp `qubn5UjuOdU3rnOodQ6x` linked to the pipeline+contact; the GHL UI shows the opp on the contact page; and a contact re-pull shows it in `opportunities[]`. FK verified **both directions**.
> - **Opportunities New→Push ✅** — local create got remote id back.
> - **Contacts New→Push ❌ (FLAG)** — `scratch-local-contact-0605.json` (firstName/lastName/email) → publish prints "Published 1 connection(s)" but the contact gets **no remote id**, is **not in GHL**, and stays in `files unpublished` as "added". Reproducible. Opportunity create works + contact edit works, so it's specific to **contact create** — a record-level failure swallowed by a connection-level "success". Needs `/investigate` (connector `createContact`, a required GHL field, or a swallowed per-record error).
> - Delete→Push, custom fields, Pass 2 boundary values: not yet.

### Connector facts (from ENDPOINTS.md — read before testing)
- Base host `https://services.leadconnectorhq.com`; **mandatory header `Version: 2021-07-28` on every request**.
- PIT is **scoped to one Location**; most endpoints need the Location ID but **param name/placement is inconsistent** (Contacts: `locationId` in body; Opportunities: `location_id` snake_case; everything else: `locationId` query).
- **Pagination is per-entity, not standardized** (searchAfter / startAfter+startAfterId / page / skip / offset / conversations-cursor).
- **Per-endpoint `limit` caps differ and the OpenAPI docs are wrong.** Exceeding a cap returns `422` that **silently fails the whole folder pull** (empty folder, job still reports success). Verified caps: proposals=20, surveys=50, blog authors/categories=50; the rest accept 100.

Legend: ✅ verified in GHL · ⬜ not yet · ➖ N/A (read-only) · ❌ broken.

## Objects / entity types — what the connector exposes
Best-case future-state contract (`Status` = built/planned), not just what's wired today. GHL is **MIXED**: static entities + per-Location custom fields + **custom objects**.

### 1. Structural entities — define the record path in Scratch
**Record path:** `/{Entity}/{record}.json`. A Private Integration Token is scoped to **one Location** (sub-account), so the Location is the **connection scope, not a path segment** — the Entity is the only path segment (`basePath = []`). Compare Airtable `/{base}/{table}/{record}.json`.

| Structural entity | Role | Path segment? |
|---|---|---|
| Location (sub-account) | the connection scope; the PIT is scoped to one | — (the connection, not in path) |
| **Entity** (Contacts/Opportunities/Custom Object/…) | the table — its records are files | **path segment** (the table itself) |

### 2. Main entities — independent top-level record types → each its own Scratch table
Custom **Objects** (user-defined record types) are entities → here. Custom **fields** are columns (see [the dynamic part](#custom-fields--custom-objects-the-dynamic-part--untested)), not entities. Writable: Contacts, Opportunities, Custom Object records; the rest are reference/read-only.

| Entity | Scratch table | Pull | Create | Edit | Delete | FK | Status |
|---|---|:--:|:--:|:--:|:--:|:--:|---|
| Contacts | record | ✅ | ✅ | ✅ | ⬜ | ⬜ | **built** (New→Push ❌ — see flag) |
| Opportunities | record | ✅ | ✅ | ⬜ | ⬜ | ✅ both ways | **built** |
| Custom Objects | record (dynamic) | ⬜ | ⬜ | ⬜ | ⬜ | ⬜ | **built** (code); untested |
| Users | read-only reference (`GET /users/`) | ✅ | ➖ | ➖ | ➖ | — | **built** (pull, read-only) |
| Pipelines | read-only reference (no get-by-id) | ✅ | ➖ | ➖ | ➖ | — | **built** (read-only) |
| Calendars / Campaigns / Conversations / Forms / Trigger Links / Products / Proposals / Surveys / Workflows / Blog Authors / Blog Categories | read-only reference | ⬜ | ➖ | ➖ | ➖ | — | **built** (code, read-only); pull untested |
| Funnels / Payments / Email Templates | top-level reference (not yet built) | ⬜ | ➖ | ➖ | ➖ | — | planned (read-only) |

### 3. Scoped / non-top-level entities
GHL's per-contact (or per-calendar) sub-resources — **scoped to a parent**, reached only through it; they belong in the parent's deep fetch, not a top-level table.

| Entity | Why not top-level | How we reach it | Status |
|---|---|---|---|
| Notes | **scoped** — `GET /contacts/{id}/notes`, no global list | embed into each Contact's deep fetch | planned |
| Tasks | **scoped** — `GET /contacts/{id}/tasks` | embed into Contact deep fetch | planned |
| Appointments / Calendar Events | **scoped** — per contact / per calendar | per-parent fetch → embed | planned |
| Associations | **scoped** — relations per record (the `/associations` API) | per-parent; out of scope in v1 | planned |

> Structural facts for GHL are captured under [Connector facts](#connector-facts-from-endpointsmd--read-before-testing) and [Edge cases](#edge-cases-discovered-from-endpointsmd---re-verify-live): Location-scoped PIT, mandatory `Version` header, per-entity valueKey/scoping-param/pagination/limit-caps, custom-object values in a keyed `properties` bag.

## Entities × Operations

Writable per the API: **Contacts** (CRUD+upsert), **Opportunities** (CRUD+status), **Custom Object records**. Everything else is reference/config → **pull-only**.

| Entity | Pull | Create→Pull | Edit→Push | New→Push | Delete | FK | Service-UI create path |
|--------|:----:|:-----------:|:---------:|:--------:|:------:|----|------------------------|
| Contacts | ✅ | ✅ | ✅ | ✅ | ⬜ | ⬜ | Contacts → **Add Contact** (top-right) → drawer (First/Last/email/phone) → Save |
| Opportunities | ✅ | ⬜ | ⬜ | ✅ | ⬜ | ✅ **write+read** `pipelineId`→Pipelines, `contactId`→Contacts | Opportunities → Add (pick pipeline/stage) |
| Custom Objects | ⬜ | ⬜ | ⬜ | ⬜ | ⬜ | ⬜ | Settings → Objects → record |
| Pipelines | ✅ | ➖ | ➖ | ➖ | ➖ | — | (reference; no get-by-id — re-fetches all) |
| Calendars | ⬜ | ➖ | ➖ | ➖ | ➖ | — | (read-only) |
| Calendar Groups | ⬜ | ➖ | ➖ | ➖ | ➖ | — | (read-only) |
| Campaigns | ⬜ | ➖ | ➖ | ➖ | ➖ | — | (read-only) |
| Conversations | ⬜ | ➖ | ➖ | ➖ | ➖ | — | (read-only; cursor pagination ⚠️) |
| Forms | ⬜ | ➖ | ➖ | ➖ | ➖ | — | (read-only) |
| Trigger Links | ⬜ | ➖ | ➖ | ➖ | ➖ | — | (read-only) |
| Products | ⬜ | ➖ | ➖ | ➖ | ➖ | — | (read-only; some custom fields) |
| Proposals | ⬜ | ➖ | ➖ | ➖ | ➖ | — | (read-only; limit cap **20**) |
| Surveys | ⬜ | ➖ | ➖ | ➖ | ➖ | — | (read-only; limit cap **50**) |
| Users | ✅ | ➖ | ➖ | ➖ | ➖ | — | (read-only) |
| Workflows | ⬜ | ➖ | ➖ | ➖ | ➖ | — | (read-only) |
| Blog Authors | ⬜ | ➖ | ➖ | ➖ | ➖ | — | (read-only; limit cap **50**) |
| Blog Categories | ⬜ | ➖ | ➖ | ➖ | ➖ | — | (read-only; limit cap **50**) |

Not yet built — but **classified, not dropped** (see the Objects tables above, best-case future state): Tasks / Notes / Appointments / Calendar Events / Associations are **scoped** sub-entities (table 3, planned); Funnels / Payments / Email Templates are top-level reference entities (table 2, planned).

## Custom fields / Custom Objects (the dynamic part) — untested
Distinction: a custom **field** is a **column on an entity** (Contacts/Opportunities) → field-types, not an entity. A custom **object** is a user-defined **record type** → it's a main entity (table 2 above).
- Contacts/Opportunities custom fields discovered from `GET /locations/{locationId}/customFields` (filtered by `model`). Stored verbatim as `{ id, <valueKey> }` — **valueKey is `value` for Contacts, `fieldValue` for Opportunities**. dataTypes: TEXT, LARGE_TEXT, NUMERICAL, PHONE, MONETORY, SINGLE_OPTIONS, MULTIPLE_OPTIONS, DATE, CHECKBOX, FILE_UPLOAD, RADIO, EMAIL. ⬜ add one of each in GHL → pull → edit → push.
- Custom Object record values live in a keyed **`properties`** bag (by `fieldKey`). ⬜

## Foreign keys / associations
- `Opportunities.pipelineId` → Pipelines · `Opportunities.contactId` → Contacts (annotated FKs). write ⬜ / read ⬜
- No general association endpoint implemented (the `/associations/` API is out of scope).

## Edge cases discovered (from ENDPOINTS.md; ⬜ = re-verify live)
- **`limit` cap → silent empty pull.** Over-cap = `422` that fails the whole folder but the job reports success. Probe each new entity's real cap at 100.
- **Per-entity pagination** is bespoke (searchAfter/startAfter/page/skip/offset/cursor). Contacts `searchAfter` is the unverified one (>100 contacts) and is **stripped from each record before storage** (transport, not data).
- **Opportunities** is the only endpoint using `location_id` snake_case; its customFields valueKey differs from Contacts.
- **Pipelines** has no get-by-id; `pullRecordFilesByIds` re-fetches all and filters.

## Gotchas
- The general Scratch/CLI ones apply — see `docs/connector-build.md`. Publish flow: `files accept → files upload → files publish`; confirm pushes in the GHL API, not the pull; `cliCanPublish` gate.

## Bugs found + fixed (2026-06-05)
- **Contacts create silently lost (FIXED).** Root cause was NOT in this connector: the publish plan-build resolved a file's DataFolder by `(workbookId, path)` only (`SchemaHelperService.getDataFolderInfo`). This test workbook has two `/Contacts` folders (GHL + a generic-api `quo`/OpenPhone connection), so the GHL contact ops were tagged with the generic-api folder's schema (`wsId:"GET"`), and the GHL connector dispatched the create to `POST /objects/GET/records` → 400 "Invalid Key Passed". Fix: scope the lookup + cache by `connectorAccountId` (`schema-helper.service.ts` + `publish-plan-build.service.ts`); regression test `schema-helper.data-folder-collision.spec.ts`. Verified: contact create now lands (`POST /contacts/` → 201).
- **GHL errors were opaque (IMPROVED).** Added an api-client response interceptor so HighLevel's real error body surfaces (was just "Request failed with status code 400"); e.g. duplicate-email now reads "This location does not allow duplicated contacts."
- **Still open (separate, DEV-10175):** the CLI prints "Published 1 connection(s)" even when a record-level op fails (`completed-with-errors`). Check `files unpublished` / `PublishPlanOperation.status` to catch it until the CLI summary is fixed.

## Open issues
- (none filed)

## UI quick-links (time-savers for the gstack browser)
Location-scoped; sub-account id `57eAggUmMecWhpP8kkis`. **Verify/refine these on the first browser pass** (paths below are the expected HighLevel v2 shape — confirm and correct).

| Where | URL |
|-------|-----|
| Login | `https://app.gohighlevel.com/` ✅ (logged in: white-label "MOGXP", WhaleSync / Redmond WA) |
| Contacts list | `https://app.gohighlevel.com/v2/location/57eAggUmMecWhpP8kkis/contacts/smart_list/All` ✅ confirmed |
| Contact detail | `https://app.gohighlevel.com/v2/location/57eAggUmMecWhpP8kkis/contacts/detail/<contactId>` ✅ confirmed |
| Private Integration Token settings | `.../settings/private-integrations` (verify) |
| Custom fields settings | `.../settings/custom_fields` (verify) |
| Opportunities | `.../opportunities/list` (verify) |
| Billing | `.../settings/billing` (verify) |

Note: navigating straight to a deep link returns HTTP 404 on first load but the SPA hydrates fine — ignore the 404, wait for render.
