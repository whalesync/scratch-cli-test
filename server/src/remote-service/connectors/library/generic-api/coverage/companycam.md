<!-- ⛔ DO NOT DELETE THIS FILE. It is generated and maintained by the `/test-generic-connector`
     skill as the resumable record of how well the GENERIC_API connector covers CompanyCam.
     Update it in place. One file per service, at
     server/src/remote-service/connectors/library/generic-api/coverage/<service>.md -->

# `CompanyCam` — GENERIC_API coverage

> **Do not delete this file.** Generated by `/test-generic-connector`; the resumable record of what's been tested for this service through the read-only generic connector. The connector itself never branches on this service — see the [Improvement candidates](#improvement-candidates) generality gate.

## Metadata
- **Template version:** 2026-06-12 — reconciled to the current `service-coverage-template.md` (includes the User-notes section, added 2026-06-12).
- **Last run:** `2026-06-12` · `live fetch` · Tester: `Ivan Dimitrov`
- **Service:** `CompanyCam` · API type: `rest`

Legend: ✅ fetched & verified · ⬜ not yet · ➖ N/A · ❌ not fetchable (see reason).

## User notes (client-facing brief)
A short, sendable brief lives as a separate standalone doc — don't duplicate it here.

> Brief: [`companycam-user-notes.md`](./companycam-user-notes.md).

## Milestones
Status: ✅ done · 🔄 in progress · ⬜ not started.

| # | Milestone | Status | Notes |
|---|---|:--:|---|
| 1 | **Key obtained** (human logged into service, long-lived key generated) | ✅ | Long-lived bearer PAT — NOT OAuth-only → supported |
| 2 | **Connection created** (GENERIC_API connection, auth probe passed) | ✅ | `coa_HOZ9cqDane`, healthStatus OK; via `setup-generic-connection.sh` |
| 3 | **First fetch** (≥1 entity pulled, counts verified) | ✅ | driver + server probe + real worker pull all match the UI |
| 4 | **All entities classified** (every endpoint fetchable ✅ or unsupported ❌) | ✅ | 8/9 top-level fetchable; ChecklistTemplates ❌ |
| 5 | **Seed→fetch loop verified** (seeded more, re-pulled) | ✅ | seeded +4 projects / +3 tags / +1 group / +2 photos (via API, with permission) → re-pull matched 5 / 22 / 1 / 3 |
| 6 | **Improvement candidates triaged** (each GENERAL / TRIVIAL / UNSUPPORTED) | ✅ | 1 GENERAL → PLAN.md; rest UNSUPPORTED/no-change |

## Service & connection
- **Login:** `https://app.companycam.com` · **API docs:** `https://docs.companycam.com` (reference index also at `https://companycam.readme.io/llms.txt`).
- **API key source:** CompanyCam web app → profile menu → **Access Tokens** (`https://app.companycam.com/access_tokens`) → **+ New Token** → (leave "read only" unchecked) → **Create Token**, then copy. Requires **Admin** role; available on **all plans**. The token is a **long-lived personal access token** (no expiry tied to a login session). **Never record the key itself.** To inspect a live one, decrypt `ConnectorAccount.encryptedCredentials` (AES-256-GCM, key `ENCRYPTION_MASTER_KEY` in `server/.env`, AAD `connector-account`; `server/tools/decrypt-credentials.js`).
- **Accounts used:** initial run on a "Whalesync Testing" trial; that trial expired (whole account went read-only), so the run moved to a fresh "Whaleform Inc" trial — a **new Access Token** was generated and swapped onto the same connection via `PATCH …/connections/:id` `{userProvidedParams:{apiKey}}`. Connector behavior was identical across both accounts (incl. the ChecklistTemplates 401), confirming findings aren't account-specific.
- **Auth header style:** `bearer` (`Authorization: Bearer <token>`).
- **API traits:** pagination `page` (`?page=N&per_page=M`, terminates on empty page; also supports `modified_since` for incremental and `query` filters) · OAuth-only? **no** (PAT available; OAuth exists only for partner apps) · base URL `https://api.companycam.com/v2`. List responses are **bare top-level JSON arrays** (Rails-style) — apiget handles these natively. Record IDs are **strings** at `id`.
- **Feature flag:** `ENABLE_GENERIC_CONNECTOR` enabled for test user? **yes** (confirmed by user).

## Connection setup
- **Workbook:** `wkb_p1oWekVWXe` ("generic-companycam") · **ConnectorAccount:** `coa_HOZ9cqDane` · created `2026-06-12` via `setup-generic-connection.sh`.
- **Server:** parallel session **N=3** — `http://localhost:3013` (monolith on this branch, own Redis `spinner-redis-3` on `:6382`), per [`/start-parallel-session`](../../../../../../../.claude/skills/start-parallel-session/SKILL.md).
- **`extras` used** (auth header style + endpoints; the testable surface; also mirrored at `server/src/scripts/apiget-fixtures/companycam.json`):
```jsonc
{
  "apiType": "rest",
  "authHeader": { "style": "bearer" },
  "endpoints": [
    { "id": "ep_companycam_projects",            "name": "Projects",           "method": "GET", "url": "https://api.companycam.com/v2/projects?page=1&per_page=100" },
    { "id": "ep_companycam_photos",              "name": "Photos",             "method": "GET", "url": "https://api.companycam.com/v2/photos?page=1&per_page=100" },
    { "id": "ep_companycam_users",               "name": "Users",              "method": "GET", "url": "https://api.companycam.com/v2/users?page=1&per_page=100" },
    { "id": "ep_companycam_groups",              "name": "Groups",             "method": "GET", "url": "https://api.companycam.com/v2/groups?page=1&per_page=100" },
    { "id": "ep_companycam_tags",                "name": "Tags",               "method": "GET", "url": "https://api.companycam.com/v2/tags?page=1&per_page=100" },
    { "id": "ep_companycam_videos",              "name": "Videos",             "method": "GET", "url": "https://api.companycam.com/v2/videos?page=1&per_page=100" },
    { "id": "ep_companycam_webhooks",            "name": "Webhooks",           "method": "GET", "url": "https://api.companycam.com/v2/webhooks?page=1&per_page=100" },
    { "id": "ep_companycam_checklists",          "name": "Checklists",         "method": "GET", "url": "https://api.companycam.com/v2/checklists?page=1&per_page=100" },
    { "id": "ep_companycam_checklist_templates", "name": "ChecklistTemplates", "method": "GET", "url": "https://api.companycam.com/v2/checklist_templates?page=1&per_page=100" }
  ]
}
```
- **No `overrides` needed** for any endpoint — apiget auto-detects `page` pagination from `page`/`per_page` and reads the bare top-level array; `idPath` defaults to `id`.

## Entities
Top-level collections (the generic, no-parent-id endpoints). **Maximized** — every top-level collection CompanyCam exposes.

| Entity | Method + URL | Pagination | `idPath` | Fetchable | Records | Notes |
|---|---|---|---|:--:|--:|---|
| Projects | `GET /v2/projects` | `page` | `id` | ✅ | 5 | rich nested fields (address, coordinates, integrations, primary_contact); stored verbatim; 25-field schema |
| Photos | `GET /v2/photos` | `page` | `id` | ✅ | 3 | was 0 → seeded → re-probe infers **17 fields** (`uris`, `coordinates`, `project_id`, `creator_id`, …). Schema needs ≥1 record. |
| Users | `GET /v2/users` | `page` | `id` | ✅ | 1 | the acting user; `company_id` ref |
| Groups | `GET /v2/groups` | `page` | `id` | ✅ | 1 | seeded "Scratch Crew" |
| Tags | `GET /v2/tags` | `page` | `id` | ✅ | 22 | ~19 CompanyCam default tags + 3 seeded; pagination walked page 1 + empty page 2 |
| Videos | `GET /v2/videos` | `page` | `id` | ✅ | 0 | fetches (empty); pagination/idPath detected. Records are mobile-app-only — not API-seedable. |
| Webhooks | `GET /v2/webhooks` | `page` | `id` | ✅ | 0 | fetches (empty); config object, not user content |
| Checklists | `GET /v2/checklists` | `page` | `id` | ✅ | 0 | fetched 1 on the prior account (refs `project_id`, `creator_id`, `checklist_template_id`); new account has none (created from templates in UI) |
| ChecklistTemplates | `GET /v2/checklist_templates` | `page` | `id` | ❌ | – | **302 → `/users/sign_in` → 401** (see Fetchability); not token-accessible |

**Sub-resource collections** (require a parent id in the path — e.g. `GET /v2/projects/{id}/comments`). The generic connector has **no path templating / fan-out**, so these are **not** general entities. They ARE fetchable when pinned to a concrete parent URL (one endpoint per parent). Verified live against project `105997447`:

| Sub-resource (pinned to project `105997447`) | HTTP | Records | Note |
|---|:--:|--:|---|
| `…/projects/{id}/comments` | 200 | 0 | bare array — fetchable via pinned URL ✅ |
| `…/projects/{id}/photos` | 200 | 0 | bare array — fetchable via pinned URL ✅ |
| `…/projects/{id}/documents` | 200 | 0 | bare array — fetchable via pinned URL ✅ |
| `…/projects/{id}/labels` | 200 | 0 | bare array — fetchable via pinned URL ✅ |
| `…/projects/{id}/checklists` | 403 | – | CompanyCam per-endpoint quirk; use top-level `/checklists` instead |

Other documented sub-resources (not separately tested): `…/projects/{id}/videos`, `…/projects/{id}/assigned_users`, `…/projects/{id}/collaborators`, `…/projects/{id}/invitations`, `…/photos/{id}/comments`, `…/photos/{id}/tags`.

## Fetchability
For every ❌, the reason mapped to the connector's hard limits, plus any `overrides` workaround.

| Entity | ✅/❌ | Reason (if ❌) | Workaround tried |
|---|:--:|---|---|
| Projects, Photos, Users, Groups, Tags, Videos, Webhooks, Checklists | ✅ | — | none needed (auto-detect) |
| ChecklistTemplates | ❌ | **Not token-accessible.** `GET /v2/checklist_templates` returns `302 → https://api.companycam.com/users/sign_in` (Rails web-session auth). apiget follows the same-host redirect; the sign-in route returns `401 {"error":{"general":"Bad credentials"}}`. The same bearer token returns 200 on all 8 other endpoints — this is a CompanyCam-side per-endpoint quirk, not a connector defect. | None viable. `overrides` can't fix an auth/route gap. **Declared UNSUPPORTED.** |
| Sub-resource collections (templated) | ➖ | Require a parent id in the path; connector has no FK/templating. | **Fetchable via a pinned full URL** (one endpoint per parent) — verified 200 + bare array above. Not a general entity. |

## Reference fields (pseudo-FKs)
The generic connector has **no foreign-key support** — recorded as data observations only.

| Entity.field | Looks like a reference to | Notes |
|---|---|---|
| `project.creator_id` | Users | raw string id; `creator_type` = "User" |
| `project.company_id` | (Company — no entity endpoint) | tenant id; present on most entities |
| `checklist.project_id` | Projects | |
| `checklist.creator_id` | Users | |
| `checklist.checklist_template_id` | ChecklistTemplates | target entity is ❌ unsupported |
| `tag.company_id` | (Company) | |
| `user.company_id` | (Company) | |
| `photo.project_id` | Projects | confirmed after seeding photos |
| `photo.creator_id` | Users | `creator_type` = "User" |
| `photo.company_id` | (Company) | |

## Fetch verification log
The read-only proof: timestamped seed→fetch entries.

- `[2026-06-12 14:55]` **Baseline driver fetch (all 9 endpoints)** via `apiget:driver companycam` → Projects 1, Photos 0, Users 1, Groups 0, Tags 19, Videos 0, Webhooks 0, Checklists 1 ✅ (all auto-detected `page` pagination, `idField=id`); ChecklistTemplates ❌ HTTP 401. Counts match the CompanyCam UI (Whalesync org: 1 project "First Project", 0 photos, 1 checklist).
- `[2026-06-12 15:01]` **Server-side probe** (`probe-endpoint`, real decrypt + apiget + schema inference) for Projects / Photos / Checklists → `page` pagination, `idPath=id`, inferred schema 25 / 0 / 13 fields; page 1 then empty page 2 (correct termination). Photos schema empty (0 records).
- `[2026-06-12 15:02]` **Real worker pull** (probe → `data-folder/create` with probe → `scratchmd linked pull`) for 5 folders → Projects 1, Photos 0, Tags 19, Users 1, Checklists 1. Counts match. Spot-checked record file `CompanyCam/Projects/105997447.json` = **verbatim CompanyCam JSON**, filename = record `id`.
- `[2026-06-12 15:29]` **Account switch:** initial trial ("Whalesync Testing") expired → account read-only. Moved to a fresh "Whaleform Inc" trial; generated a new Access Token in the UI and swapped it onto the same connection via `PATCH …/connections/coa_HOZ9cqDane`. New token verified (`/projects` → 200).
- `[2026-06-12 15:30]` **Seed (via CompanyCam API, with user permission)** — `POST` created 4 projects, 3 tags, 1 group ("Scratch Crew"), 2 photos (on a seeded project; `placehold.co` image URIs). User had manually added 1 photo earlier. Note: Videos + Checklists are **not API-seedable** here (mobile-app / template-driven) — left empty.
- `[2026-06-12 15:31]` **Seed→fetch re-pull (connector worker)** → Projects **5**, Photos **3**, Tags **22**, Users **1**, Groups **1**, Checklists **0**. Counts match the live API exactly. ✅
- `[2026-06-12 15:30]` **Photos schema inference confirmed:** re-probe after seeding → page 1 = 3 records, inferred schema **17 fields** (`uris`, `coordinates`, `project_id`, `creator_id`, `captured_at`, …). (Was 0 fields when the table was empty.) ✅

## Improvement candidates
Each run through the **generality gate**. Default to UNSUPPORTED when unsure; burden of proof is on adding connector code.

| Candidate | Gap | Classification | Rationale (how many services it'd help) | Action |
|---|---|---|---|---|
| **CLI generic table-create needs the probe step** | `scratchmd linked add` for a GENERIC_API connection fails 500 "Endpoint … has not been probed yet" — the CLI's create path never runs the probe, so generic tables (and connections — `extras` is dropped) can only be set up via the web UI / direct API. | **GENERAL** | Affects **every** generic-connector CLI user, every service — nothing CompanyCam-specific. | → `PLAN.md` item (`FOR_REVIEW`) |
| ChecklistTemplates 302→sign_in | One documented endpoint isn't exposed to the bearer-token API. | **TOO-SPECIFIC** | Only CompanyCam, and only this endpoint. | → **declared UNSUPPORTED** (no connector change) |
| Clearer "redirect to login" error | apiget follows a 3xx to `/users/sign_in` and surfaces an opaque `401 Bad credentials`. A message naming the redirect-to-login would aid debugging. | **TOO-SPECIFIC** (for now) | Plausible on any Rails/Devise API with mixed web+API routes, but only 1 service has hit it. Revisit if ≥2 more do. | → noted, no change |
| Sub-resource fan-out / path templating | Sub-resources need a parent id; no templating. | **TOO-SPECIFIC** (out of scope) | This is effectively FK support, which the connector deliberately omits. Sub-resources already work via a pinned full URL. | → no change |

## Coverage summary
- **Top-level entities:** 8 fetchable ✅ / 1 unsupported ❌ (ChecklistTemplates) / 0 untested.
- **Sub-resources:** fetchable via pinned parent URL (not as templated entities).
- **Overall:** **well-covered.** CompanyCam is a clean fit for the generic REST connector — page pagination + bare-array responses + string `id` are all auto-detected with zero `overrides`. The only top-level gap is one endpoint CompanyCam doesn't expose to API tokens.
