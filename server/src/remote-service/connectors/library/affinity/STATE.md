<!-- ⛔ DO NOT DELETE THIS FILE. It is generated and maintained by the `/connector-build`
     skill as the resumable record of this connector's test coverage. The connector
     work itself may need it to extend/finish the connector. Update it in place. -->

> ℹ️ **No trial — production connector.** Affinity has been live (read-only) since 2023. There is no paid trial to cancel; this doc was created by **adopting the existing human-built code** (no STATE.md existed). Coverage cells start `⬜` because nothing has been re-verified through this skill's CLI+service loop — code existing is **not** proof of a passing round-trip.

# Affinity — Connector Test Coverage

> **Do not delete this file.** Generated by `/connector-build`; the resumable record of what's been tested. Future work to extend the connector relies on it.

## Test account (used to run this coverage)
- **Service:** Affinity (CRM / relationship-intelligence for VC & dealflow) · service constant `AFFINITY` · login at <https://<your-org>.affinity.co>
- **Trial:** No trial / paid customer accounts (this is a live production connector)
- **API key / credentials:** Affinity **v2 API key** (single `apiKey` param, `Bearer` auth). Generated in Affinity at **Settings → Manage Apps → New App**. To inspect a live connection: decrypt `ConnectorAccount.encryptedCredentials` for `<coa_id>` (AES-256-GCM, key `ENCRYPTION_MASTER_KEY` in server/.env, AAD `connector-account`; see `server/tools/decrypt-credentials.js`). **No dedicated skill test account provisioned yet** — TODO before live coverage.
- **Scratch side:** workbook `<wkb_id>` / connection `<coa_id>` · local workspace `<path>` — *none set up for skill runs yet.*
- **Auth method:** `user_provided_params` (API key) — **CLI-connectable**: `scratchmd connections ... add --service AFFINITY --param apiKey=<k>`. No OAuth offered by Affinity (confirm — see Milestone 9 / TODOs).
- **Provenance:** Adopted from existing human-built code on 2026-06-10 (cold-read; see [SKILL.md → Adopting a human-built connector](/.claude/skills/connector-build/SKILL.md)).

## Metadata
- **Type:** STATIC · fixed entities **+ user-defined Lists discovered dynamically** + per-list custom fields (**mixed**)
- **Template version:** 2026-06-10 — the `coverage-template.md` version this STATE.md is reconciled to. **On resume, compare against the template's current `Template version`; if this is older, apply every newer [Template changelog] entry to bring this doc's structure up to date, then bump this value.**
- **Last run:** 2026-06-10 · doc-only adoption (code cold-read; no live CLI/gstack run) · Tester: Ivan Dimitrov

Legend: ✅ verified · ⬜ not yet · ➖ N/A · ❌ broken.

## Milestones — where this connector is in the build
At-a-glance progress. Status: ✅ done · 🔄 in progress · ⬜ not started. **All cells below are from this skill's perspective** — the connector is live in prod, but nothing here has been re-verified through the CLI+service loop.

| # | Milestone | Status | Notes |
|---|---|:--:|---|
| 1 | **Account ready** (registered / logged into the service web app) | ⬜ | No dedicated skill test account provisioned. Connector is live in production. |
| 2 | **Connected** (connection created, health OK) | ⬜ | Not connected via this skill's harness. CLI-connectable (API key). |
| 3 | **First fetch** (pulled ≥1 record) | ⬜ | Pull code is implemented for all tables; not re-run here. |
| 4 | **All entities seeded & fetched** (every main entity has a record, pulled in) | ⬜ | |
| 5 | **Full write CRUD** (create + edit + delete exercised, push) | ⬜ | **Connector is read-only by design** — `createRecords`/`updateRecords`/`deleteRecords` throw. → **DEV-10298** (add publishing). |
| 6 | **Foreign keys tested** (CLI move parent→parent) | ⬜ | No `x-scratch-foreign-key` declared yet (see TODOs). |
| 7 | **Edge cases & quirks tested** (Pass 2 tricky parts) | ⬜ | |
| 8 | **View(s) built** (default view; fields grouped by existing service mechanics) | 🔄 | Default views built in code for all tables (`affinity-default-view.ts`, shipped via DEV-10107); **not re-verified in desktop** this review. |
| 9 | **OAuth** (final / pre-release — client created with user, *or* requirements documented) | ➖ | **N/A — confirmed.** Affinity's REST API (v1 + v2) is **API-key / Bearer only**, no OAuth (v2 docs, 2026-06-10). OAuth exists only for Affinity's separate **MCP server**, not the API. Nothing to implement. |

## TODOs — known pending tasks
Seeded from the code cold-read + linked issues. Check off as they land.

- [ ] **Add write support (publishing)** — connector is read-only at **both** layers: `createRecords`/`updateRecords`/`deleteRecords` throw `AffinityError` (`affinity-connector.ts:370-382`), and the API client issues **only `GET`**. → **DEV-10298** (High, S). Good news: the common edit→push (field-value updates) **is now supported on v2** with the same Bearer token; whole-record create/delete still needs v1. Full surface + phased plan in [Publishing / write support](#publishing--write-support-dev-10298) below.
- [ ] **Audit read-only labeling before enabling writes** — confirm enriched / relationship-intelligence / computed fields are `x-scratch-readonly` so publish doesn't attempt them.
- [ ] **Declare FKs** for relationship writes (person/company/list-entry references).
- [ ] **Declare foreign keys** — no `x-scratch-foreign-key` anywhere. Person/company/opportunity reference field values, list-entry `listId`, and entity-file `person_id`/`organization_id`/`opportunity_id` should be declared so FK linking + the [Stage D](/.claude/skills/connector-build/SKILL.md) tests work.
- [ ] **Incremental polling** — only cursor *resume* scaffolding exists (`progress.cursor`); no last-modified field, no `x-scratch-last-modified-field`, no `incrementalPullSupport`, no delta filtering. **DEV-10159 (incremental) was canceled** — confirm whether Affinity v2 exposes a modified-since filter at all; if not, document as unsupported.
- [ ] **Audit read-only field labeling** — verify every computed/enriched/system field is `x-scratch-readonly` (enriched fields, relationship-intelligence values, timestamps, ids). Matters once writes land so publish doesn't silently drop edits.
- [ ] **Opportunities are thin** — v2 returns only `id`/`name`/`listId` (no `/v2/opportunities/fields`). Custom opportunity values exist only per-list via list-entries. Decide whether/how to enrich the tenant Opportunities table.
- [ ] **Users / teammates entity** — not exposed. Decide whether to sync internal Affinity users (near-universal reference entity).
- [ ] **Reminders entity** — Affinity has Reminders; not exposed. Classify as planned main entity or out-of-scope.
- [ ] **Per-list entity files** — only workspace-wide `/entity-files` (v1) is exposed; per-list attachments are not pulled.
- [ ] **Run live coverage passes** — provision/confirm a test account, connect via CLI, pull every table, then flip the `⬜` cells (Milestones 1-4, matrix Pull column) to ✅.
- [ ] **Re-verify default views in desktop** (Milestone 8) — confirm banner groups span the right columns.
- [x] **Confirm OAuth availability** (Milestone 9) — confirmed 2026-06-10: REST API is API-key/Bearer only, no OAuth (OAuth exists only for Affinity's separate MCP server). Milestone 9 → ➖ N/A.

## Objects / entity types — what the connector exposes  (best-case future state — Status tracks built/planned)

### 1. Structural entities — define the record path in Scratch
**Record paths:** tenant tables at root → `/{Table}/{record}.json`; user Lists grouped under a folder → `/Lists/{List name}/{record}.json`.

| Structural entity | Role | Path segment or grouping? |
|---|---|---|
| (workspace / org) | the account the API key belongs to — always singular for a connection | connection scope — **not** in the path |
| `Lists` | groups all user-created list tables | `parentPath` grouping (`basePath: ['Lists']`) |
| List (e.g. "Deal Flow") | a user-created list; its entries are records | path segment (the list name) |

### 2. Main entities — independent top-level record types → each its own Scratch table

| Entity | Scratch table | Pull | Create→Push | Edit→Push | Delete | FK | Status |
|---|---|:--:|:--:|:--:|:--:|:--:|---|
| Persons | `People` (`/People/…`) | ⬜ | ➖ | ➖ | ➖ | ⬜ | built — read-only (dynamic fields via `/v2/persons/fields`) |
| Companies | `Companies` | ⬜ | ➖ | ➖ | ➖ | ⬜ | built — read-only (dynamic fields via `/v2/companies/fields`) |
| Opportunities | `Opportunities` | ⬜ | ➖ | ➖ | ➖ | ⬜ | built — read-only, **thin** (id/name/listId; no fields endpoint) |
| Notes | `Notes` | ⬜ | ➖ | ➖ | ➖ | ⬜ | built — read-only (fixed schema; embeds person/company/opp previews) |
| Entity Files (attachments) | `Entity Files` | ⬜ | ➖ | ➖ | ➖ | ⬜ | built — read-only, **v1 API** `/entity-files` (workspace-wide only) |
| User Lists (each list) | `/Lists/{name}/…` | ⬜ | ➖ | ➖ | ➖ | ⬜ | built — read-only (dynamic fields via `/v2/lists/{id}/fields`) |
| Users / teammates | — | ⬜ | — | — | — | — | **planned** — not exposed (decide whether to sync) |
| Reminders | — | ⬜ | — | — | — | — | **planned** — Affinity entity, not exposed |

> Write columns are `➖` because the connector is **read-only today** (all writes throw); they become testable cells once **DEV-10298** lands.

### 3. Scoped / non-top-level entities — not directly fetchable as a top-level entity

| Entity | Why not top-level | How we reach it | Status |
|---|---|---|---|
| List / persons / companies **field definitions** | schema, not records | discovery endpoints `/v2/lists/{id}/fields`, `/v2/persons/fields`, `/v2/companies/fields` (array → object keyed by field id) | built (drives dynamic schema) |
| Interactions (relationship-intelligence) | not a standalone record; values attached to fields | embedded in person/company field values (`interaction` valueType) | built (embedded, read-only) |
| Note → related persons/companies/opportunities | embedded previews, not full records | `includes=personsPreview,companiesPreview,opportunitiesPreview` on `/v2/notes` | built (preview embeds) |
| Per-list entity files / attachments | no per-list list endpoint used | only workspace-wide v1 `/entity-files` implemented | planned |

<!-- ✅ in a push column requires a MANUAL on-disk edit pushed via the CLI (accept→upload→publish) then confirmed in the service. Not applicable until writes exist. -->

## Entities × Operations  (STATIC / mixed)

| Entity | Pull | Create→Pull | Edit→Push | New→Push | Delete | FK | Service-UI create path (for when writes land) |
|--------|:----:|:-----------:|:---------:|:--------:|:------:|----|------------------------|
| People | ⬜ | ⬜ | ➖ | ➖ | ➖ | ⬜ | Affinity → People → Add person |
| Companies | ⬜ | ⬜ | ➖ | ➖ | ➖ | ⬜ | Affinity → Companies → Add company |
| Opportunities | ⬜ | ⬜ | ➖ | ➖ | ➖ | ⬜ | Created inside a deal/opportunity List |
| Notes | ⬜ | ⬜ | ➖ | ➖ | ➖ | ⬜ | Note added on a person/company/opportunity |
| Entity Files | ⬜ | ⬜ | ➖ | ➖ | ➖ | ⬜ | File attached to an entity (v1 only) |
| List entries (per List) | ⬜ | ⬜ | ➖ | ➖ | ➖ | ⬜ | Add a row to the List in Affinity |

## Field-types × Operations  (DYNAMIC / mixed — Affinity `valueType`s)
Custom-field values carry a `valueType` (~17 variants in `AffinityFieldMetadata`); schema is discovered per entity/list and annotated `x-scratch-connector-data-type` + `x-scratch-remote-field-id`. Edit/New are `➖` until writes land.

| Field type (valueType) | Pull | Edit→Push | New→Push | Notes |
|------------|:----:|:---------:|:--------:|-------|
| text / filterable-text | ⬜ | ➖ | ➖ | |
| number / floating-point | ⬜ | ➖ | ➖ | |
| datetime | ⬜ | ➖ | ➖ | tz handling to confirm |
| dropdown (single-select) | ⬜ | ➖ | ➖ | id vs label to confirm |
| ranked-dropdown | ⬜ | ➖ | ➖ | Affinity-specific (status/stage) |
| dropdown-multi (multi-select) | ⬜ | ➖ | ➖ | |
| person / person-multi | ⬜ | ➖ | ➖ | implicit FK → People |
| company / company-multi | ⬜ | ➖ | ➖ | implicit FK → Companies |
| location | ⬜ | ➖ | ➖ | expanded into banner-group subfields in default view |
| interaction (relationship-intelligence) | ⬜ | ➖ | ➖ | read-only / enriched |
| enriched / global (computed) | ⬜ | ➖ | ➖ | read-only by source |

## Bulk operation limits / pagination

| Operation | Max per request | Mechanism | Notes |
|---|---|---|---|
| Read (list) — v2 | 100 records/page (`PAGE_LIMIT`) | cursor (`pagination.nextUrl` → `cursor`) | persons/companies/opps/notes/list-entries |
| Read (list) — v1 (Entity Files) | 500 records/page (`V1_PAGE_SIZE`) | token (`next_page_token`) | `/entity-files` only |
| Create / Update / Delete | — | — | not supported (read-only); revisit with DEV-10298 |

- **Rate limits:** connector registers a `RateLimiterSpec` of **10 points / 1s**; 429 responses are retried honoring `Retry-After`. Org/key quota readable via `getApiQuota()` → `GET /rate-limit` (returns `api_key_per_minute` + `org_monthly`), with a `dashboardUrl`.

## Incremental polling
- **Supported:** **NO (currently).** Only cursor *resume* checkpointing exists (`pullRecordFiles` threads `progress.cursor`); there is no last-modified driver field, no `x-scratch-last-modified-field`, and no `incrementalPullSupport` declaration. Each pull is a full fetch.
- **Mechanism:** n/a — no `since`/modified-since filter wired.
- **Deletions:** not detected on incremental (no incremental).
- **Note:** DEV-10159 ("Affinity incremental polling") was **canceled**. First confirm whether the Affinity v2 API even exposes a modified-since filter before attempting.

## Endpoints (what the connector calls)
Base host: `https://api.affinity.co` · auth: `Authorization: Bearer <apiKey>`.
- **API version & client:** reads on **v2** (`/v2/...`, the latest major) via a hand-rolled axios client (`createApiClient`); Entity Files + `/rate-limit` on **v1**, same Bearer token. No official SDK (house default). **Currency: up to date on reads; writes not implemented** (see [Publishing / write support](#publishing--write-support-dev-10298)).

| Entity / area | Op | Method + path | Note |
|---|---|---|---|
| Persons | list / get | `GET /v2/persons` · `GET /v2/persons/{id}` | `fieldTypes=enriched,global,relationship-intelligence` |
| Persons | discover | `GET /v2/persons/fields` | field metadata → dynamic schema |
| Companies | list / get | `GET /v2/companies` · `GET /v2/companies/{id}` | tenant field types |
| Companies | discover | `GET /v2/companies/fields` | |
| Opportunities | list / get | `GET /v2/opportunities` · `GET /v2/opportunities/{id}` | no fieldTypes; thin (id/name/listId) |
| Notes | list / get | `GET /v2/notes` · `GET /v2/notes/{id}` | `includes=companiesPreview,personsPreview,opportunitiesPreview,repliesCount` |
| Entity Files | list / get | `GET /entity-files` · `GET /entity-files/{id}` | **v1 API**, token pagination |
| Lists | list / get | `GET /v2/lists` · `GET /v2/lists/{id}` | discovers user list tables (cached) |
| List entries | list / get | `GET /v2/lists/{id}/list-entries` · `GET /v2/lists/{id}/list-entries/{entryId}` | `fieldTypes=list,enriched,global,relationship-intelligence` |
| List | discover | `GET /v2/lists/{id}/fields` | per-list field metadata |
| Connection test | — | `GET /v2/lists?limit=1` | `testConnection()` |
| Quota | — | `GET /rate-limit` (v1-era, no `/v2`) | `getApiQuota()` |

## Publishing / write support (DEV-10298)
Read-only today (writes throw; API client is `GET`-only). Research (Affinity **v2** docs + Whalesync's direct **v1** connector at `../whalesync/api/bottlenose/src/connectors/affinity-connector/`) shows publishing is **partly v2-native, partly v1-only**:

**Supported on v2 now (same Bearer token, several BETA) — do these first:**
- **Edit a field value** (the common edit→push): `POST /v2/{persons|companies}/{id}/fields/{fieldId}` (single) · `PATCH /v2/{persons|companies}/{id}/fields` (batch, ≤100 fields) · list entries `…/list-entries/{id}/fields[/{fieldId}]`. Body is a **typed value object** `{ "value": { "type": "<field-type>", "data": … } }`, ids for refs/dropdowns (e.g. `{"type":"company-multi","data":[{"id":1}]}`) — matches the read-side reindex-by-field-id model.
- **Notes**: `POST /v2/notes` (create) · update · `DELETE` — full CRUD. **Lists**: create a list; CRUD a list field's dropdown options. **Reminders**: create.

**NOT in v2 — needs v1** (Whalesync's proven path; v1 uses Basic auth with the key as password — or test whether the v2 Bearer token authorizes v1, as it already does for `/entity-files`):
- **Create / delete whole records** (persons, companies, opportunities) → v1 `POST`/`DELETE /persons|/organizations|/opportunities`. So **New→Push and Delete→Push of records are v1-only.**
- **Opportunities**: no v2 writes at all (read-only); v1 only, and create requires `list_id`.
- **List membership** (add/remove a record to a list): v1 `POST`/`DELETE /lists/{id}/list-entries`.
- v1 quirks to carry over: multi-value field = one field-value row each (diff add/remove); ranked-dropdown writes the option **id** not text; note update is `content`-only; strict monthly API quota (each edit can be several calls).

**Plan:** Phase 1 = v2-native field-value edits + Notes CRUD (covers the "edit existing records" ask for DEV-10298, no new auth). Phase 2 = add a v1 codepath for record create/delete, opportunities, and list membership only if customers need them.

## Foreign keys / associations
No `x-scratch-foreign-key` declared yet — associations are currently embedded as nested values / id references. Declaring them is a TODO.

| FK field → target table | Read (pull) | Write via CLI (move parent→parent) | Notes |
|---|:--:|:--:|---|
| person / person-multi field → `People` | ⬜ | ➖ | implicit (valueType), not declared |
| company / company-multi field → `Companies` | ⬜ | ➖ | implicit (valueType), not declared |
| list entry `listId` → its List | ⬜ | ➖ | |
| `entity-files.person_id/organization_id/opportunity_id` → People/Companies/Opportunities | ⬜ | ➖ | simple nullable numbers |

## Edge cases discovered
- (none recorded yet — adoption was doc-only; see [SKILL.md → Stage E] for what to hunt for once live)

## Gotchas
- **Two API versions in one connector:** persons/companies/opportunities/notes/lists use **v2** (cursor pagination); Entity Files uses **v1** (`next_page_token`). Watch for shape/auth differences between them.
- **Opportunities are intentionally thin** (id/name/listId) — v2 has no `/opportunities/fields`; their custom values live per-list via list-entries.
- **Field values are an array reindexed to an object keyed by field `id`** before storage; values are addressed by remote field id (`x-scratch-remote-field-id`), not by name.
- **`/rate-limit` has no `/v2` prefix** (v1-era endpoint), unlike the rest of the v2 surface.

## Open issues
- **DEV-10298** — Update Affinity connector to support publishing (read-only → read-write). *Backlog, High.*
- **DEV-10159** — Affinity incremental polling. *Canceled* (revisit feasibility).

## OAuth (final milestone — create the client with the user, or document what it takes)
- **Requires:** Affinity authenticates via a **v2 API key** (Settings → Manage Apps → New App), not OAuth. No developer "OAuth app" flow is known to be offered.
- **Endpoints:** n/a (no authorize/token URLs) — API-key only.
- **App / client:** n/a.
- **Status:** **N/A — confirmed (2026-06-10).** Affinity's REST API (v1 + v2) authenticates by **API key / Bearer only**; there is no OAuth, no client_id/secret, no authorize/token flow. (The only OAuth in Affinity's ecosystem is its separate **MCP server** at `mcp.affinity.co` — for LLM clients, not the REST API the connector uses.)
- **Blockers:** none — API-key auth fully covers connect + all testing.

## UI quick-links (time-savers for the gstack browser)

| Where | URL |
|-------|-----|
| Service login | `https://<your-org>.affinity.co` |
| API key / token settings | Affinity → Settings → **Manage Apps → New App** |
| OAuth app / developer console | n/a (API-key auth) |
| API reference (v2) | `https://developer.affinity.co/` (v2) · legacy v1 `https://api-docs.affinity.co/` |
| People list | `https://<your-org>.affinity.co/persons` |
| Companies list | `https://<your-org>.affinity.co/companies` |
| Lists | `https://<your-org>.affinity.co/lists` |
