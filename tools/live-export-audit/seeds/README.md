# Per-service seed scripts (created by /test-live-export runs)

One directory per source service; scripts must be idempotent (fable_qa_* naming) and
readable by the review skill to re-seed identically. Never store credentials here.
